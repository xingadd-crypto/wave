/// Single source of truth for the app version string shown in the UI and used
/// by the OTA update check (compare against the manifest). Keep in sync with
/// `pubspec.yaml`'s `version:` on every release bump.
const String appVersion = '1.0.28';